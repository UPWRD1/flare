with "examples/io"

let add of x: int, y: int -> int = 
    return x + y

let main =
    a = 3
    b = 5
    result = add(a,b)
    IO.new().print_int(a).print_str(" + ").print_int(b).print_str(" = ").print_int(result).out("")

