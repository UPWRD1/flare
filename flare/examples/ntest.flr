--with io
-- with "examples/vec"
-- with "examples/wrapper"
--with container

-- with lambdacalc

with option

let add: (l: int, r: int) -> int =
    l + r

let main: () -> bool =
    a = Option.Some{3}
    a.is_some()

