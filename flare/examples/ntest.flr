package Main = 
    # let id x : ?T -> ?T =
        # x

    # let person_age p : {age: num} -> num =
            # p.age

    # let x_getter r : {x: ?T} -> ?T =
        # r.x

    let combiner a b : ?T -> ?U -> {x: ?T, y: ?U} =
        {x = a, y = b}

    let main : {x: num, y: str} = 
        # let rx = {x = 4} in 
            combiner 3 "asdf" 
               
