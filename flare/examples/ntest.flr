--with io
-- with "examples/vec"
-- with "examples/wrapper"
--with container

with lambdacalc

type Catagory = enum of
    A{int},
    B,
    C

let add: (l: int, r: int) -> int =
    l + r

let main: () -> int =
    a = Catagory.:A{3}
    a.0
