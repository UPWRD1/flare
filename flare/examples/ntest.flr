package Main = 
    # let id x : ?T -> ?T =
        # x

    # let person_age p : {age: num} -> num =
            # p.age

    let x_getter r : {x: ?T} -> ?T =
        r.x

    let combiner a b : ?T -> ?U -> {x: ?T, y: ?U} =
        {x = a, y = b}

    let main: {x: str, y: {x: bool, y: @ok}} =
         let temp = combiner true @ok in
        # let rx = {x = 4} in 
           combiner "asdf" temp
               
