 pub Main: num = {
    x = 3
    y = x
    double x : num -> num = x * 2
    
    factorial x : num -> num =
        x as {
            0 = 1
            x = x + factorial (x - 1)
        }

    main = double y
}.main
   
